{
  "name": "playwright-demoqa-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "debug": "cross-env PWDEBUG=1 playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "cross-env": "^7.0.3"
  }
}
